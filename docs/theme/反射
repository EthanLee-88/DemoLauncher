
  / ********************** java反射 ************************/
  java反射机制是指在程序运行状态中，可以构造任意一个类的对象，乐意了解任意一个对象所属的类，
  可以了解任意一个类的成员变量、方法等，可以调用任意一个对象的属性。
  这种动态获取类的对象、动态调用对象的功能称为java的反射机制。

  要通过反射获取一个类的信息，首先需要获取该类对应的Class对象，该对象在JVM进行类加载时创建。
  该对象所属的类提供了大量的方法来获取相应的类信息。